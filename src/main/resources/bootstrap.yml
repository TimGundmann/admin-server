server:
  port: 2222
  use-forward-headers: true

spring:
  application:
    name: admin
  boot:
    admin:
      context-path: admin
      ui:
        public-url: https://gundmann.dk/admin
  cloud:
    config:
      uri: http://192.168.1.100:5678
